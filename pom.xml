<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>manfred.hearth</groupId>
    <artifactId>hearth</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>
    <name>hearth</name>

    <modules>
        <module>hearth-domain</module>
        <module>hearth-app</module>
        <module>hearth-infrastructure</module>
        <module>hearth-adapter</module>
        <module>hearth-start</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring.boot.version>4.1.0</spring.boot.version>
        <cola.version>4.3.2</cola.version>
        <mybatis.plus.version>3.5.17</mybatis.plus.version>
        <sort.skip>true</sort.skip>
        <testcontainers.version>1.21.4</testcontainers.version>
        <owasp.html.sanitizer.version>20260313.1</owasp.html.sanitizer.version>
        <commonmark.version>0.30.0</commonmark.version>
        <bucket4j.version>8.19.0</bucket4j.version>
        <jacoco.version>0.8.15</jacoco.version>
        <mockito.version>5.23.0</mockito.version>
        <byte-buddy.version>1.18.10</byte-buddy.version>
        <archunit.version>1.5.0</archunit.version>
        <coverage.data-file>${maven.multiModuleProjectDirectory}/hearth-start/target/jacoco-merged.exec</coverage.data-file>
        <coverage.check.skip>true</coverage.check.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Testcontainers BOM 放在 Spring Boot 之前，覆盖其中的旧版本。
                 1.21.4 默认探测 Docker API 1.44，兼容 Docker Engine 29。-->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cola</groupId>
                <artifactId>cola-component-dto</artifactId>
                <version>${cola.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cola</groupId>
                <artifactId>cola-component-exception</artifactId>
                <version>${cola.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis.plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser-4.9</artifactId>
                <version>${mybatis.plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.commonmark</groupId>
                <artifactId>commonmark</artifactId>
                <version>${commonmark.version}</version>
            </dependency>
            <dependency>
                <groupId>org.commonmark</groupId>
                <artifactId>commonmark-ext-gfm-tables</artifactId>
                <version>${commonmark.version}</version>
            </dependency>
            <dependency>
                <groupId>com.googlecode.owasp-java-html-sanitizer</groupId>
                <artifactId>owasp-java-html-sanitizer</artifactId>
                <version>${owasp.html.sanitizer.version}</version>
            </dependency>
            <dependency>
                <groupId>manfred.hearth</groupId>
                <artifactId>hearth-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>manfred.hearth</groupId>
                <artifactId>hearth-app</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>manfred.hearth</groupId>
                <artifactId>hearth-infrastructure</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>manfred.hearth</groupId>
                <artifactId>hearth-adapter</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- JUnit 6 no longer brings the launcher transitively. Surefire resolves it
             while discovering tests, so declare it in every module's inherited test runtime. -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>3.1.1</version>
                    <configuration>
                        <tagNameFormat>v@{project.version}</tagNameFormat>
                        <autoVersionSubmodules>true</autoVersionSubmodules>
                        <pushChanges>false</pushChanges>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>25</source>
                        <target>25</target>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>1.18.40</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>3.28.0</version>
                    <configuration>
                        <rulesets>
                            <ruleset>${maven.multiModuleProjectDirectory}/config/pmd/pmd-ruleset.xml</ruleset>
                        </rulesets>
                        <targetJdk>25</targetJdk>
                        <failOnViolation>true</failOnViolation>
                        <printFailingErrors>true</printFailingErrors>
                        <linkXRef>false</linkXRef>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                    <dependencies>
                        <!-- Resolve the JVM agent before Surefire forks tests, including from a fresh local repository. -->
                        <dependency>
                            <groupId>net.bytebuddy</groupId>
                            <artifactId>byte-buddy-agent</artifactId>
                            <version>${byte-buddy.version}</version>
                        </dependency>
                    </dependencies>
                    <configuration>
                        <argLine>${argLine} -Xshare:off --enable-native-access=ALL-UNNAMED -javaagent:${settings.localRepository}/net/bytebuddy/byte-buddy-agent/${byte-buddy.version}/byte-buddy-agent-${byte-buddy.version}.jar</argLine>
                        <excludes>
                            <exclude>**/*IT.java</exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <executions>
                    <execution>
                        <id>pmd-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <scm>
        <connection>scm:git:git@github.com:manfredma/hearth.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/manfredma/hearth.git</developerConnection>
        <url>https://github.com/manfredma/hearth</url>
    <tag>v0.1.0</tag>
    </scm>

    <profiles>
        <!--
            跨进程依赖只能在 staging 验证；默认 Maven 生命周期保持为可离线执行的单元测试。
        -->
        <profile>
            <id>staging-integration</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>3.2.5</version>
                        <dependencies>
                            <!-- Resolve the JVM agent before Failsafe forks staging integration tests. -->
                            <dependency>
                                <groupId>net.bytebuddy</groupId>
                                <artifactId>byte-buddy-agent</artifactId>
                                <version>${byte-buddy.version}</version>
                            </dependency>
                        </dependencies>
                        <configuration>
                            <!--
                                Spring Boot repackage replaces the main artifact with a fat jar.
                                Failsafe must load application classes from target/classes instead.
                            -->
                            <classesDirectory>${project.build.outputDirectory}</classesDirectory>
                            <includes>
                                <include>**/*IT.java</include>
                            </includes>
                            <argLine>-Xmx256m ${argLine} -Xshare:off --enable-native-access=ALL-UNNAMED -javaagent:${maven.repo.local}/net/bytebuddy/byte-buddy-agent/${byte-buddy.version}/byte-buddy-agent-${byte-buddy.version}.jar</argLine>
                            <systemPropertyVariables>
                                <hearth.it.redis.password>${env.HEARTH_IT_REDIS_PASSWORD}</hearth.it.redis.password>
                            </systemPropertyVariables>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <skipTests>true</skipTests>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!--
            仅在显式指定 coverage.includes 时启用。JaCoCo 会对匹配的生产类要求
            行、分支和方法均零遗漏；避免历史未覆盖代码阻断与本次改动无关的提交。
        -->
        <profile>
            <id>changed-coverage</id>
            <activation>
                <property>
                    <name>coverage.includes</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>${jacoco.version}</version>
                        <executions>
                            <execution>
                                <id>prepare-agent</id>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>check-changed-classes</id>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                                <configuration>
                                    <skip>${coverage.check.skip}</skip>
                                    <dataFile>${coverage.data-file}</dataFile>
                                    <includes>
                                        <include>${coverage.includes}</include>
                                    </includes>
                                    <rules>
                                        <rule>
                                            <element>CLASS</element>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>MISSEDCOUNT</value>
                                                    <maximum>0</maximum>
                                                </limit>
                                                <limit>
                                                    <counter>BRANCH</counter>
                                                    <value>MISSEDCOUNT</value>
                                                    <maximum>0</maximum>
                                                </limit>
                                                <limit>
                                                    <counter>METHOD</counter>
                                                    <value>MISSEDCOUNT</value>
                                                    <maximum>0</maximum>
                                                </limit>
                                            </limits>
                                        </rule>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
