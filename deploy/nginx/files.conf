events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;
        server_name _;

        location /images/ {
            alias /srv/images/;
            autoindex off;
            add_header X-Content-Type-Options nosniff always;
            expires 7d;
        }
    }
}
