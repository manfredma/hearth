# Hearth 管理端布局

管理端由 React 统一渲染，入口为 `hearth-start/src/main/frontend/main.jsx`，样式与组件同目录隔离。桌面端使用固定侧栏和内容区，移动端侧栏变为抽屉并提供遮罩和关闭按钮。

当前原型包含总览、应用接入、安全设置三个导航入口。新增页面必须保持主壳层、视觉变量、键盘焦点和移动端断点一致；业务组件不得通过全局样式影响其他页面。
